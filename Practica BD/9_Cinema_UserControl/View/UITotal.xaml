<UserControl
    x:Class="_9_Cinema_UserControl.View.UITotal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_9_Cinema_UserControl.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="uctotal">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" Text="Seient: " Width="55"></TextBlock>
        <TextBlock Grid.Column="1" x:Name="txbNumCad" Text="{Binding ElementName=uctotal, Path=id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="30" ></TextBlock>
        <TextBlock Grid.Column="2" Text="Preu: " Width="50"></TextBlock>
        <TextBlock Grid.Column="3" x:Name="txbPreuCad" Text="{Binding ElementName=uctotal, Path=preu, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="30"></TextBlock>
    </Grid>
</UserControl>
